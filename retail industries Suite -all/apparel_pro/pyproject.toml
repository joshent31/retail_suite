[project]
name = "apparel_pro"
authors = [
    { name = "Your Organization", email = "admin@example.com"}
]
description = "Style masters, size charts, production tracking, QC and returns for apparel retail on ERPNext."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
