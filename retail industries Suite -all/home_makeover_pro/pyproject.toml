[project]
name = "home_makeover_pro"
authors = [
    { name = "Retail Industry Suite", email = "admin@example.com"}
]
description = "End-to-end project tracking, room measurements, material estimation, contractor assignment and milestone billing for home makeover/interiors businesses on ERPNext."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
