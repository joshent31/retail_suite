[project]
name = "footwear_pro"
authors = [
    { name = "Your Organization", email = "admin@example.com"}
]
description = "Style masters, size runs, material BOM, QC, warranty claims and vendor ratings for footwear retail on ERPNext."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
