[project]
name = "fmcg_pro"
authors = [
    { name = "Retail Industry Suite", email = "admin@example.com"}
]
description = "Batch/expiry tracking, distributor master, trade schemes, route/beat planning and van sales for FMCG distribution on ERPNext."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
